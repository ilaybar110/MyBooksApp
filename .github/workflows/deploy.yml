# Deployment is handled by Railway (railway.app)
# Railway auto-deploys on every push to main:
#   1. runs `npm run build` (builds frontend into frontend/dist)
#   2. runs `npm start` (starts backend which serves the frontend)
name: placeholder
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy handled by Railway"
